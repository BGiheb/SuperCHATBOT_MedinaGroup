// Update this page (the content is just a fallback if you fail to update the page)

const Index = () => {
  // Redirect to login for the admin interface
  window.location.href = '/login';
  return null;
};

export default Index;
